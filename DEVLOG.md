# Development Log

## 2026-07-02 - Initial Setup
- Set up the project folders (src, notebooks, docker, data, results)
- Set up Python environment (venv + requirements.txt)
- Set up Docker so anyone can run the project the same way
- Confirmed everything runs (Jupyter works inside Docker)