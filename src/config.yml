seed: 2026

simulation:
  n_trials_per_subject: 1000
  max_t: 3.0
  dt: 0.02

priors:
  nu:
    distribution: gamma
    shape: 5
    scale: 0.5
  alpha1:
    distribution: gamma
    shape: 5
    scale: 0.2
  tau:
    distribution: exponential
    scale: 0.15

paths:
  data_dir: ../data
  results_dir: ../results